#include <iostream>
#include <string>
#include <vector>
using namespace std;

double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2);
int main(int argc, const char *argv[])
{
    std::vector<int> n1;
    std::vector<int> n2;
    n1.push_back(2);
    findMedianSortedArrays(n1,n2);
    return 0;
}


double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2)
{
    int length1 = nums1.size();
    int length2 = nums2.size();

    return 1;
}

void findpos(int s,int e)
{
}
